# 동적 T-패리티 불변량 (모든 폭)

MSE 80246의 가설(퍼펙트 클리어 시 T 개수는 짝수)은 거짓. 줄 클리어가
체커보드 색 할당을 뒤집기 때문. 아래가 그 자리에 들어가는 올바른 일반형.

## 기호

    φ(B) = Σ_{(r,c) ∈ B} (-1)^(r+c)          r = 바닥부터 센 행

조각별 기여: T만 ±2, 나머지 6종은 모든 회전에서 0 (흑2 백2).

클리어 이벤트 하나가 줄 j_1 < ... < j_k 를 지운다고 하자.
지워진 줄들 사이의 잔여 구간을 "블록"이라 하고, 블록 b 아래에 있는
지워진 줄의 수를 s_b 라 한다. 블록 b 는 s_b 칸 내려오므로 부호가 (-1)^(s_b) 배.

    D = Σ_i (줄 j_i 위의 칸 수, 단 꽉 찬 줄은 제외)
    A = Σ_i φ(줄 j_i) = (w 홀수면) Σ_i (-1)^(j_i),  (w 짝수면) 0

w 짝수에서 A = 0 인 이유: 꽉 찬 줄의 φ 는 Σ_{c=0}^{w-1} (-1)^(j+c) 이고
안쪽 합이 w 짝수일 때 0.

## 정리 (per-event)

    Δφ_clear  ≡  -2D - A   (mod 4)

증명 스케치:
    φ_pre  = Σ_b φ_b + Σ_i φ(줄 j_i)
    φ_post = Σ_b (-1)^(s_b) φ_b
    Δφ     = Σ_b ((-1)^(s_b) - 1) φ_b - A = -2 Σ_{s_b 홀수} φ_b - A
임의의 집합에서 칸 수 ≡ φ (mod 2) 이고, 블록 b 는 자기 아래 s_b 개의
j_i 각각에 대해 한 번씩 세어지므로  D ≡ Σ_b s_b φ_b ≡ Σ_{s_b 홀수} φ_b (mod 2).
따라서 2D ≡ 2 Σ_{s_b 홀수} φ_b (mod 4).                          [QED]

## 따름정리 (global)

빈 보드 -> 퍼펙트 클리어이므로 Σ Δφ = 0. T 만 ±2 를 기여하므로

    Σ_clears Δφ_clear  ≡  2 · #T   (mod 4)

그리고 Σ_clears A 는 항상 짝수. 2로 나누면 사람이 읽는 형태:

    #T  ≡  Σ_clears D  +  (Σ_clears A)/2    (mod 2)

w 짝수면 A = 0 이므로

    #T  ≡  Σ_clears (지워진 줄 위에 남은 칸 수)    (mod 2)

클리어가 없거나 모두 짝수 칸을 위에 남기면 우변 0 -> #T 짝수.
**원래 MSE 가설은 이 정리의 특수한 경우다.**

## 전수 검증

모든 퍼펙트 클리어 플레이 수열을 dedup 없이 완전 열거하고 네 항목을 검사.

    w   n   PC 수열      per-event   global   sum(A) 홀수   readable
    4   2         5          0          0          0           0
    4   3        55          0          0          0           0
    4   4      1105          0          0          0           0
    5   5     14970          0          0          0           0
    6   3        30          0          0          0           0
    6   6    244010          0          0          0           0
    7   7   4498382          0          0          0           0
    8   2         2          0          0          0           0
    8   4       246          0          0          0           0
                -------
    합계    4758805          0          0          0           0

w=5 가 포함돼 홀수 폭의 A 보정항도 검증됨.

**w=7 은 2026-09-19 에 닫혔다.** 4n = Rw 때문에 w=7 은 n 이 7 의 배수여야 해서
최소 케이스가 (7,7) 이고, 그래서 혼자 450 만 개다 — 나머지 여덟 케이스를 합친 것의 17 배.
이걸로 전수 범위가 폭 4·5·6·7·8 이 됐다.

**단, 독립 구현은 아직 w=7 을 안 덮는다.** `verify.cpp` 만 4,498,382 을 셌고
`averify.py 7 7` 은 따로 돌고 있다. "독립 구현 둘이 개수까지 일치" 는 260,423 범위의 주장이다.

## 폐기된 중간 형태 (기록용)

초기에 세웠던 corr = Δφ/2 는 w 홀수에서 깨진다. 꽉 찬 줄이 w 칸(홀수)이라
Δφ 가 홀수일 수 있어 정수가 아님. w=5 에서 48568 건의 "불일치"가 나왔는데
전부 이 정의 오류였고 반례가 아니었다. mod 4 로 올려서 해결.

또 A 를 폭과 무관하게 Σ(-1)^(j_i) 로 두면 짝수 폭에서 틀린다.
꽉 찬 줄의 φ 기여는 (-1)^j · Σ_c (-1)^c 이고 짝수 폭에서 후자가 0.

## 재현

    make                      # bin/verify 까지 전부 빌드
    ./bin/verify <w> <n> [piecemask]
    make check                # 위 표의 가벼운 항목 전부
    make check-full           # w=6 n=6 (244,010) 까지
    ./bin/verify 7 7          # 4,498,382. check 에는 안 들어 있다 (오래 걸림)

출력 네 항목이 전부 0 이어야 한다. 2026-09-15 에 표 전건을, 2026-09-19 에 w=7 을 재현했다.

**독립 구현 교차검증 (2026-09-16 복구).** 2026-09-15 감사 때 "C++ 와 Python 독립 구현이
수열 개수까지 일치" 주장의 Python 프로그램이 레포에 없어 철회했었다.
`src/py/averify.py` 로 다시 썼고 개수가 맞는다.

    ./bin/verify 와 averify.py 의 PC 수열 개수
    w=4 n=2·3·4   5 / 55 / 1105        일치
    w=5 n=5       14,970               일치
    w=6 n=3       30                   일치
    w=8 n=2·4     2 / 246              일치
    w=6 n=6       244,010              일치
    합계          260,423              자리까지 일치

verify.cpp 와 겹치지 않게 썼다.

  - 보드: __int128 비트마스크 -> 행별 정수 마스크의 튜플
  - 착지 행: 충돌 탐침 -> 열 높이 프로파일 산술 r = max(h[c] - bottom[c])
    (테트로미노는 각 열에서 칸이 연속이므로 정확하다)
  - D: 줄별 합 -> "자기 아래 지워진 줄 수가 홀수인 칸의 개수"

마지막 항목이 뜻밖의 소득이다. 두 D 정의가 mod 2 로 같다는 것이 per-event 항등식에
실제로 필요한 단계인데, 두 구현이 서로 다른 정의를 쓰므로 그 일치가 전 수열에서
기계 검증된다 (불일치 0). 위 §기호 에서 Sigma_i 를 강조한 바로 그 자리다.

## 남은 것

1. ~~w=7 n=7 전수 검증~~ — **2026-09-19 완료** (4,498,382, 네 항목 0).
   독립 구현 `averify.py 7 7` 은 아직. w=9 n=9 는 안 했다 (w=9 는 n 이 9 의 배수라
   최소 케이스가 (9,9) 이고 w=7 보다 훨씬 크다)
2. per-event 정리는 위 스케치로 사실상 증명됨. 논문 형태로 정서
3. 7-bag 제약 하에서의 형태 (조각 순서 제약이 D 에 주는 영향)
