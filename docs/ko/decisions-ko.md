<!-- 볼트 Decisions.md 의 전시 사본. 원본을 고치고 이쪽으로 복사한다. -->
# Decisions

최신이 위. 한 항목 = 한 결정. 유형: `오류` `수정` `추가` `보류` `충돌` `정책`.
"왜 이렇게 했는가" 가 빠진 항목은 항목이 아니다.

---

## 2026-09-15 | 정책 | 이 세션의 환경과 브랜치

- 발견: 세션 시작 점검. CLAUDE.md 4 장은 Windows + Claude Desktop + Obsidian MCP 를
  전제하지만 이 세션은 원격 Linux 컨테이너(Claude Code on the web)다. Obsidian MCP·RAG 가 없다.
- 근거: 볼트가 git 클론 `/home/user/DevVault` 로 올라와 있어 filesystem 으로 직접 읽고 쓸 수 있다.
  그래서 "MCP write 는 3KB 이하" 제약은 이 세션에 적용되지 않는다.
- 조치: 볼트 읽기·쓰기 전부 filesystem 으로. 볼트 검색은 grep/Read 로 대체.
  또 CLAUDE.md 4 장은 "main 에 직접 커밋, 브랜치 안 만든다" 인데 이 세션의 운영 지침이
  두 레포 모두 `claude/intelligent-bohr-ejzybx` 브랜치로 개발·푸시하라고 지정했다.
  운영 지침이 상위이므로 그쪽을 따랐다. **볼트 변경도 main 이 아니라 그 브랜치에 있다.**
- 영향: 레포, 볼트. 재혁이 로컬 Obsidian(main)에 병합해야 볼트 원본에 반영된다.
- 상태: 해결 (병합은 재혁 몫)

## 2026-09-15 | 정책 | CLAUDE.md 가 레포에 없다

- 발견: 9 장 검산 규칙 사본을 갱신하려다 파일을 못 찾음.
- 근거: `/home/user/DevVault/CLAUDE.md`, `/home/user/tetris-dynamic-tilings/CLAUDE.md` 둘 다 없음.
  세션 프롬프트로만 주어졌다 = 재혁 로컬 전용 파일.
- 조치: 볼트 TODO §검산 규칙에 새 규칙 3 개를 추가했으나 **CLAUDE.md 9 장 사본은 갱신하지 못했다.**
  CLAUDE.md 를 레포(또는 볼트)에 두면 원격 세션도 같은 규약을 읽을 수 있다 — 제안.
- 영향: 없음 (보고 사항)
- 상태: 보류 (재혁 판단 대기)

## 2026-09-15 | 추가 | Decisions.md 와 검산 대장.md 생성

- 발견: CLAUDE.md 6.6-6. 둘 다 없었다.
- 근거: 이번 세션에 기록할 내용이 생겼으므로 Startup.md 규약("내용이 있을 때만 만든다")을 충족.
- 조치: `NOTES_DIR` 에 두 파일 생성. 검산 대장에 A·B·C·D 전 항목과 노트 해시 표를 넣었다.
- 영향: 볼트, docs (전시 사본은 다음 동기화에서)
- 상태: 해결

## 2026-09-21 | 추가 | 논문 3 판 — §7 에 스윕 결과를 반영했다

- 발견: 2 판의 §7 이 표본 100 개 시점에 멈춰 있었고, **거기 실린 "S·Z 가 둘 다 늦다"
  조건이 그 뒤 반증됐다.** 논문이 죽은 추측을 살아 있는 것처럼 싣고 있었다.
- 넣은 것: 보조정리(사슬 형태), 명제(인접 교환은 맨 앞에서만 대칭 — 정리 H 의 두 번째
  증명이 여기서 나온다), 정리(닫힌 가족 72 건), 명제(정적 불변량 배제),
  I 위치 표(조건 만족 180 류 전수), I 맨앞 탐침, 그리고 철회 기록.
- 철회를 지우지 않고 **어떻게 세워지고 어떻게 깨졌는지를 본문에 남겼다.**
  특히 "예측·검정이 통과했는데 왜 틀렸나" 를 적었다 — 검정에 쓴 4 건이 전부 I 1 번째,
  즉 표의 쉬운 끝이었다. 예측이 맞았다는 것이 예측한 이유가 맞았다는 뜻은 아니다.
- 정적 불변량 배제 명제가 이 절의 값이라고 본다. 같은 도착 multiset 으로 판정이
  갈리므로 **열 카운팅(보조정리 2)·부록의 T 패리티 항등식·Walkup 류가 전부 배제된다.**
  논문 자신의 도구를 논문이 배제하는 형태라 다음 사람에게 쓸모가 있다.
- 닫은 것: 180 블록의 마지막 미결 2 건(`JTILOSZ OJZS`·`OLZS`)을 길게 돌려 둘 다
  가능으로 확정했다. **180 류 × 840 = 151,200 건이 미결 0 으로 전부 닫혔다.**
  실패 92 건 / 31 류.
- 남은 위험: §7 의 "실패 94 건" 과 끝 글자 분포는 **스윕이 돌면 늘어난다.**
  본문에 "305 류 시점" 으로 못 박고 검산 대장에 `미완` 행을 뒀다. 스윕이 끝나면 갱신할 것.
- 분량: 11 쪽 → **13 쪽.** Gazette Article 목표 8~12 쪽을 넘었다. 재혁 판단 필요 —
  그대로 낼지, §7 을 줄일지, §7 을 별고로 뺄지.
- 상태: 해결 (분량 판단은 재혁 몫)

## 2026-09-21 | 추가 | I 맨앞 탐침 — 반례 0, 그리고 쫓다가 정리 하나가 또 죽었다

- 발견: I 가 맨 앞인 360 류에서 실패 0. 72 류는 840 전수(60,480 건),
  288 류는 실패를 낸 접두사 6 개로 탐침(1,728 건). CAP·TIMEOUT 0 이라 전부 확정.
  대조군 4 건이 전부 FAIL 로 상태 수까지 일치해 탐침을 믿을 수 있었다.
- 지위: **정리가 아니다.** 288 류에서 840 중 6 개 = 0.7% 다. 검산 대장에 `보류` 로 넣었다.
  한 칸 당기기가 68 개 반례로 죽은 것과는 다르다는 것까지가 지금 말할 수 있는 전부다.
- 쫓은 것: 탐침 결과에 **상태 수가 완전히 같은 묶음이 594 개** 있었다.
  정체는 **도착 2·3 번째를 바꾼 쌍**이다 (`IJOSLTZ`·`IOJSLTZ` 가 `OLSZ` 에서 417,699 동일).
  정리 H 와 똑같은 모습이라 "도착 2·3 도 판정을 보존하나" 를 25 만 건으로 검정했다.
  **일치 250,293 · 불일치 23 — 99.99% 인데 거짓이다.** 반례 넷을 솔버로 직접 확인했다.
- 뜻밖의 소득 둘:
  (1) **I 위치는 완전 불변량이 아니다.** `JTLIOSZ + LOZS` 불가능 / `JLTIOSZ + LOZS` 가능,
  **둘 다 I 가 4 번째**다. §실패 구조 I 의 위치 의 "I 위치 하나로 갈린다" 는
  꼬리 `SZO` 가족 안에서만 참이었다. 2026-09-15 에 철회된 주장이
  **결론은 맞고 근거가 틀렸던 것**으로 판명됐다 — 근거를 다른 가족에서 새로 얻었다.
  (2) **상태 수 일치와 판정 동치는 독립이다.** 도착 2·3 은 상태 수가 같은데 판정이 갈리고,
  정리 H 의 실패쌍은 판정이 같은데 상태 수가 갈렸다. 양쪽에서 확인됐다.
- 교훈: 99.99% 는 정리가 아니다. 이 프로젝트에서 죽은 관찰이 이제 다섯 개인데
  (ZS 끝 / 형태 세 가지 / 끝 글자 한 자 / S·Z 늦음 / 도착 2·3 교환)
  **전부 "표본에서 예외가 없다" 를 근거로 세운 것**이다.
  살아남은 것은 조각 집합 관찰 하나뿐이고, 그것만 대칭이 형태를 강제한다.
- 영향: 볼트 7-bag(두 절 추가, I 위치 절에 제한 명시)·검산 대장(행 5 개)·Decisions.
  레포 `data/ifirst-probe.out`(1,732 줄), `data/ifirst-cases.txt`.
- 상태: 해결

## 2026-09-21 | 확인 | 컨테이너가 실제로 재시작됐고, 푸시 고침이 값을 했다

- 발견: `uptime` 이 `up 0 min`. 스윕 세 루프와 I-맨앞 탐침이 전부 죽어 있었다.
- 손실: **없다.** 커밋 루프의 마지막 두 줄이 이랬다.

      05:53 committed 282 classes, stage2 486, push=no
      05:54 밀린 커밋 1 개 재푸시 -> 남은 0

  아침에 넣은 **주기별 재푸시가 정확히 이 상황을 잡았다.** `push=no` 로 끝났으면
  그 커밋은 재시작과 함께 사라졌다. 양쪽 레포 미푸시 0 으로 확인.
- 왜 기록하는가: 고친 이유가 가설이 아니라 **하루 안에 실제로 일어났다.**
  "커밋했으니 안전하다" 가 이 환경에서 틀린 말이라는 것의 증거다.
- 조치: 작업디렉터리(`/tmp/.../scratchpad`)는 살아남았으므로 완료 류 284 개 그대로
  재개했다. 스윕은 `sweep1260-run.sh` 한 줄(죽은 PID 는 스크립트가 알아서 정리한다),
  탐침은 같은 줄을 다시 치면 이미 끝난 건을 건너뛴다. 탐침 스크립트 머리에 그 줄을 적었다.
- 남은 위험: 탐침 출력이 작업디렉터리에만 있다. 작업디렉터리까지 날아가는 재시작이면
  탐침은 처음부터다. 오래 도는 것의 출력은 레포에 커밋해야 한다.
- 상태: 해결

## 2026-09-21 | 추가 | I 위치를 정리로 만들려다 — 진술이 거짓이었고, 대신 셋을 얻었다

- 발견: "I 가 늦으면 어렵다" 를 정리로 세우려 했는데, 자연스러운 형태인
  **"I 를 한 칸 앞으로 당기면 손해가 아니다" 가 거짓**이다. `wxyz S I Z` → `wxyz I S Z`
  72 쌍 중 **68 쌍이 가능 → 불가능**으로 뒤집히고, 양쪽 다 가능한 쌍은 0 이다.
- 근거: 처음에는 반대를 증명했다고 생각했다. 홀드 상태를 따라가 보면
  `…I S Z` 쪽이 `…S I Z` 쪽의 수를 전부 흉내낼 수 있어 보였다. **틀렸다.**
  두 수열이 공유하는 것은 "둘 중 하나를 홀드에 넣는" 가지뿐이고,
  **둘 다 놓는 가지에서 놓는 순서가 갈린다.** 하드드롭이라 순서가 보드를 바꾼다.
- 그래서 얻은 것:
  (P) **배치열의 사슬 특성화.** 홀드를 뱉는 자리들이 사슬을 이루고, 사슬 자리에는
  직전 사슬 원소가 나온다. 한 번에 하나만 미룰 수 있다는 말의 정확한 형태다.
  (Q) **인접 교환은 맨 앞에서만 대칭이다.** i=0 은 사슬 대응 `C ∖ {1}` ↔ `C ∪ {1}` 로
  보존되고 — 이것이 **정리 H 의 두 번째 증명**이다 — i ≥ 1 은 전부 깨진다.
  정리 H 가 특별했던 이유가 설명된다: 맨 앞에서만 홀드가 비어 있다.
  (R) **정적 불변량 배제.** `TJILSZO OJZS` 가능 / `TJLISZO OJZS` 불가능인데
  도착 multiset 이 같다. 놓인 조각의 multiset 만 보는 불변량은 이 문제를 결정할 수 없다 —
  동적 T-패리티도, 열 카운팅도, Walkup 류도. **어디를 파지 말아야 하는지가 정해졌다.**
- 닫힌 것: `bag1 = perm(T,J,L,O) + ISZ`, `bag2 ∈ {JOZS, JZOS, OJZS}` → **72 건 전부 불가능.**
  마지막 미결 `JOLTIZS LSOZ` 를 더 긴 시간으로 돌려 `no PC (46,029,782)` 로 확정했다.
  2 단계 파일에는 옛 TIMEOUT 줄이 남으므로 `data/resolved-extra.out` 을 나중에 읽어 덮는다.
- 검산: `src/py/plays.py` 가 규칙 재귀와 사슬 공식을 따로 계산해 4,922 비교 불일치 0,
  정리 H 는 5,008 쌍 반례 0. 대칭으로 접을 때 **거울은 bag2 도 같이 바꿔야 한다** —
  bag1 만 정규화하면 엉뚱한 줄을 읽는다 (`scripts/ipos-family.py` 에 주석으로 박아뒀다).
- 영향: 볼트 7-bag(새 절)·검산 대장(행 7 개)·TODO, 레포 `src/py/plays.py`,
  `scripts/ipos-family.py`, `data/resolved-extra.out`.
- 상태: 해결 (72 건 가족의 사람 증명은 열린 문제로 남긴다 — 도달성 논증이어야 한다)

## 2026-09-21 | 오류 | "S·Z 가 둘 다 5 번째 이후" 필요조건 추측이 반례로 죽었다

- 발견: 완료분을 집계하다 `JTZOLSI OJZS FAIL 45696247` 을 찾았다. `JTZOLSI` 는 Z 가
  **3 번째**라 조건을 어기는데 실패한다. 조건을 어기고 실패한 첫 류다.
- 검산: 2 단계 `capresolve.sh` 의 `*)` 포괄 분기(예상 밖 출력을 전부 FAIL 로 적는다)를
  먼저 의심했다. 두 솔버로 다시 돌렸다.

      fix4 JTZOLSIOJZS 200000000  ->  no PC (states 45696247)
      fix5 JTZOLSIOJZS 200000000  ->  no PC (states 45696247)

  상태 수가 한 자리도 안 틀리고 같다. fix4 는 2²⁷ 지문 키, fix5 는 완전 128 비트 키이므로
  지문 충돌도 없었다는 뜻이다. 45.7M 은 fix5 표 한계 46,976,202 **아래**라 표가 찬 수도 아니다.
  이미 증명된 정리의 예측으로 한 번 더 걸렀다 — 거울 `LTSOJZI OLSZ` 는 상태 수까지
  같은 45,696,247(보조정리 M 이 요구하는 그대로), 홀드 교환 `TJZOLSI OJZS` 는 판정만
  같고 수는 45,615,307(정리 H 가 보장하는 것이 판정뿐인 것과 일치). 반례가 맞다.
- 왜 이제야: 스윕이 조건 만족 180 류를 먼저 돌았다. 반례는 조건 위반 구간에 들어가서야 나왔다.
  2026-09-18 의 예측·검정도 통과했었는데, **검정에 쓴 미결 4 건이 전부 I 가 1 번째**였다.
  조건을 검정한 게 아니라 I 가 앞쪽인 구간을 검정한 것이었다. 순서 편향이 한 번 더 작동했다.
- 조치: 7-bag §새 관찰을 §철회로 고치고 경위를 남겼다(지우지 않았다).
  검산 대장 149 행을 `근거없음` 으로 내리고, 반례·거울·교환 검산 행 셋과
  조건 만족 180 류 I 위치별 집계 행을 넣었다. 집계를 손으로 하지 않도록
  `scripts/failstats.py` 를 만들어 재현 명령으로 걸었다.
  "끝 글자 S 또는 Z" 행의 옛 분포(S 30/Z 8/O 1)도 92 건 기준(S 59/Z 28/O 5)으로 갱신했다.
- 남은 것: 조건 만족 180 류에서 **I=5 인 12 류가 전부 실패**다. I 위치가 S·Z 위치보다
  실패를 잘 가른다. 다만 완료된 조건 위반 99 류가 **전부 I=7** 이라 아직 비율로 읽으면 안 된다.
- 영향: 볼트 7-bag·검산 대장·TODO, 레포 `scripts/failstats.py`. 논문은 이 추측을
  쓰지 않으므로 수정 없음(확인함).
- 상태: 해결

## 2026-09-21 | 오류 | 자동커밋 루프가 밤새 푸시에 실패하고 있었다 — 커밋 29 개가 안 밀렸다

- 발견: 스톱 훅이 "29 unpushed commits" 를 알렸다. 그 전까지 `autocommit.log` 에는
  `committed N classes` 만 찍혀 있어서 겉보기에는 정상이었다.
- 근거: 커밋 루프가 `for i in 1 2 3 4; do git push -q origin "$BRANCH" 2>/dev/null && break; ...`
  로 네 번 재시도한 뒤 **실패를 삼키고 다음 주기로 넘어간다.** 에러는 `/dev/null` 로 갔고
  로그에는 푸시 항목 자체가 없었다.
- 왜 중요한가: 복구 절차(`sweep1260-run.sh` 머리 주석)가 "컨테이너가 회수되면
  레포의 `data/sweep1260/` 만 남는다" 를 전제하는데, **실제로 남는 것은 커밋이 아니라
  푸시된 것**이다. 회수됐다면 밤새 돈 류 30 여 개를 통째로 다시 돌아야 했다.
- 조치: (1) 밀린 29 개를 손으로 푸시(`33e7070..08b37a8`).
  (2) 커밋 루프가 푸시 성공 여부를 `push=yes/no` 로 로그에 남기게 했다.
  (3) 매 주기 `git log origin/BR..HEAD` 를 세어 밀린 것이 있으면 다시 민다.
  (4) 커밋 루프만 교체하는 `--commit-only` 를 만들었다 — 전부 재시작하면
  1 단계가 돌던 류의 `.partial`(당시 840 중 705)을 버리기 때문이다.
- 영향: 레포 `scripts/sweep1260-run.sh`, 볼트 TODO §검산 규칙 2 줄.
- 상태: 해결 (교체 후 첫 주기에 `push=yes` 확인, 1 단계는 안 끊겼다)

## 2026-09-20 | 오류 | 논문 초고에서 증명 하나가 틀렸고 정리 하나에 폭 조건이 빠져 있었다

- 발견: 재혁이 "초고 마저 다듬어줘". 처음부터 끝까지 읽으며 검산 대장과 대조했다.
- **오류 1 — 정리 7(T) 하한, R=3 경우의 첨자.** `N_1 ≥ 2h_0 + 3q_0 ≥ 6` 이라 썼는데
  그 네 해 `(3,0,0) (2,1,0) (1,2,0) (0,3,0)` 은 전부 q_0 = 0 이라 우변이 2h_0 이 되고
  `(0,3,0)` 에서 **0** 이다. 부등식이 성립하지 않아 논증이 무너진다.
  맞는 식은 `2h_0 + 3p_0` 이고 네 해에서 6·7·8·9 다. **결론(R ≥ 4)은 그대로**이고
  R=1·2 경우와 (0,0,1) 갈래는 원래 맞게 쓰여 있었다. 순수한 오타지만 증명을 깬다.
- **오류 2 — 정리 13(부분집합 분류)에 폭 조건이 없었다.** "P 가 {I,T,J,L} 과 만나면
  가능" 을 폭 제한 없이 썼는데 정리 7·9 는 `w ≥ 4` 에서만 구성을 준다. 확인해 보니
  **T 는 w=2·3 에서 실제로 불가능**하다 (`pc3 2 2 4 0`, `pc3 3 3 4 1` 둘 다 NO).
  I 는 폭 1 부터, J·L 은 폭 2 부터 되므로 **T 만 예외**다.
  논문을 `w ≥ 4` 로 제한하고 작은 폭을 Remark 로 뺐다. 볼트 조각 분류에도 적었다.
- 흥미로운 점: 열 카운팅은 T 의 w=2·3 에서도 R=4 를 허용한다 (2 열이면 p_0 = q_0 = 1).
  **카운팅이 못 잡고 기하가 잡는 두 번째 자리**다 — 첫 번째는 S·Z 짝수 폭이었고
  논문이 따름정리 11 에서 "그런 자리는 S·Z 하나뿐" 이라고 말한다. 그 문장은 단일 조각
  **가능성**에 대한 것이라 여전히 맞지만, 작은 폭까지 보면 T 도 같은 성격의 간극을 갖는다.
- 문구·서지도 손봤다. 초록·서론 표의 "every width", 정리 13 증명의 O 프로파일 부정확,
  S·Z gap number 주장의 무출처, `refs.bib` 가 빌드에 안 쓰이는데 미확정 표시가 거기만
  있던 것, 인용 안 된 서지 셋. §7 에 거울 대칭을 더해 5040 → 1,260 류 문단을 넣었다.
- 새로 들어간 수치(작은 폭 결과, 1,260 류)는 전부 검산 대장에 줄을 만들었다 (CLAUDE.md 6.3).
- 결과: 11 쪽, 경고 0, Overfull 0. 분량 목표 8~12 쪽 안.
- 영향: 논문, 볼트 조각 분류·논문·검산 대장
- 상태: 해결

## 2026-09-20 | 오류 | 같은 자살을 네 번째로 했다 — 이번엔 규칙이 막지 못한 형태로

- 발견: 코어 배분을 2:2 로 바꾸려고 루프들을 멈추다가 `ps -eo pid,args | grep -E "...REPO=..."`
  로 PID 를 골랐다. 그 패턴이 **내 셸의 명령줄을 매치**해 내가 나를 죽였다 (exit 144).
- **규칙이 있었는데 막지 못했다.** 규칙은 `pkill -f` 와 `pgrep -f` 를 금지하고 있었고,
  나는 `ps -eo args | grep` 을 썼다. 도구 이름이 달라서 금지 목록에 안 걸렸을 뿐
  **하는 일은 똑같다 — 명령줄 전체를 매치한다.**
- 교훈: 금지 목록을 도구 이름으로 쓰면 변종에 뚫린다. 규칙을 **행위**로 다시 썼다 —
  "명령줄 전체를 매치하지 말 것. 어떤 도구를 쓰든." 그리고 안전한 형태를 같이 적었다:
  `ps -C <실행파일명>` 은 실행파일 **이름**만 보므로 셸이 안 걸린다. 실행파일이 `bash` 라
  이름으로 안 갈리면 내 PID($$)와 부모를 **먼저 제외**한 뒤 인자를 본다.
- 네 번의 이력: 2026-09-18 `pkill -f` 두 번, 09-19 `pgrep -f | kill`, 09-20 `ps -eo args | grep`.
  2026-09-19 에 "규칙을 적는 것과 지키는 것은 다르다" 고 썼는데, 이번 건은 한 단계 더 나아간다 —
  **규칙을 지켰다고 믿으면서 어겼다.** 우회로가 규칙 문구 밖에 있었기 때문이다.
- 잃은 것은 없다 (1 단계 119 류까지 커밋돼 있었고 재시작 스크립트로 복구).
- 영향: 볼트 TODO §검산 규칙, docs/ko/INDEX.md
- 상태: 해결

## 2026-09-19 | 추가 | 1,260 류 전수 착수 — 그리고 순서 편향을 밟았다

- 발견: 재혁이 "2520 × 840 전수" 를 요청. 거울 대칭까지 쓰면 **1,260 류**로 더 줄어든다.
  정리 H(교환)와 보조정리 M(거울)이 가환이고 어떤 bag1 도 고정점이 아니어서 궤도가 전부
  크기 4 다 — 기계로 확인했다 (1,260 궤도 전부 크기 4, 합쳐서 5040 을 빠짐없이 덮음).
- **거울을 실제로 검정했다.** 표본 100 개 안에 거울쌍이 없어 기존 자료로는 확인이 안 됐다.
  알려진 실패 셋의 거울상을 돌리니 전부 실패하고 **상태 수가 한 자리도 안 틀렸다**
  (38,542,538 / 37,968,211 / 48,644,898). 교환과 대비가 선명하다 — 거울은 탐색 공간
  전체의 동형사상이라 전부 훑는 실패에서 상태 수가 같고, 교환은 스태시 부분트리만
  동형이라 거기서 끝나는 성공에서 같다.
- **비용을 재고 정직하게 보고했다.** 1 류(840 건)에 9 분 28 초, 케이스당 1.93 CPU-초.
  1 단계 1,058,400 건 ≈ 6~7 일 + 2 단계 ≈ 4 일 = **10~11 일 연속**. 한 세션에 안 끝난다.
  그래서 재개 가능하게 만들고 20 분마다 자동 커밋하도록 걸었다 (`data/sweep1260/README.md`).
- **오류: 순서 편향.** 대표를 "궤도의 사전 최소" 로 잡았는데 I 가 가장 작은 글자라
  **대표가 I 로 시작하는 쪽으로 몰렸다.** 조건 만족 180 류의 앞 50 개가 전부 I 1 번째였고,
  그건 I 위치 표에서 쉬운 쪽으로 확인된 구간이다. 48 류를 돌고 나온 "실패 0" 을
  하마터면 신호로 읽을 뻔했다 — **결과가 아니라 순서의 그림자다.**
- 조치: 조건 만족 180 류를 **I 위치 내림차순**으로 재정렬했다. 알려진 실패 bag1 넷의 대표가
  30·32·40·58 번으로 올라와 대조군이 된다. 집합은 그대로고 순서만 바뀌었다.
  1 단계만 정확히 멈추고(2 단계는 살려둔 채) 재시작했다. 끝난 48 류는 건너뛴다.
- 검산 규칙 추가: **정규형이 순서에 넣는 편향을 먼저 볼 것. 알려진 답을 앞에 대조군으로 둘 것.**
  이번 프로젝트에서 반복되는 함정의 세 번째 변종이다 — CAP 을 결론으로 읽기,
  1 단계 `FAIL 0` 을 결과로 읽기, 편향된 순서의 앞부분을 표본으로 읽기.
  전부 **"아직 보지 않은 것" 을 "없는 것" 으로 읽는** 같은 실수다.
- 영향: 레포 data/, scripts/, 볼트 TODO, docs/ko/INDEX.md
- 상태: 진행 중 (1 단계 48 류 + 재정렬 후 계속, 2 단계 69 건 중 확정 중)

## 2026-09-19 | 추가 | 정리 H (홀드 교환 불변) 증명 — 관찰이 정리가 됐다

- 발견: 재혁이 증명을 요청. `fix4.cpp` 의 홀드 규칙을 그대로 읽고 시작했다
  (추측한 규칙이 아니라 우리 모델의 규칙에 대한 증명이어야 하므로).
- **보조정리 1 이 전부를 결정한다.** (P0) 은 홀드를 유지하고 (P1)·(S) 는 홀드를 채우며
  홀드는 비어 시작한다 → **홀드가 빈 구간은 앞 구간뿐이고, 스태시는 많아야 한 번**이다.
- **보조정리 2 (흡수).** L ≥ n+1 이면 모든 플레이가 k=0 스태시에 들어간다.
  `Plays_n(q) = q_1·HG(q_0; q_2…) ∪ q_0·HG(q_1; q_2…)`.
  열쇠는 "홀드를 뱉고 들어온 것을 홀드에 넣는" 수를 되풀이하면 도착 순서가 그대로
  재현된다는 것. 이때 도착 q_n 이 필요하고 **L ≥ n+1 은 여기서만 쓰인다.**
- **정리 H.** 우변이 q_0, q_1 에 대칭이므로 교환이 Plays 를 보존한다. PC 가능 여부도 같다.
- 따름: 첫 조각을 무조건 홀드에 넣어도 손해 없음 / 5040 이 동치류 2,520 개로 접힘 /
  나쁜 bag1 은 짝수 개 / 표본 100 개의 실질 문제는 98 개.
- **가설이 날카로운지 확인했다.** L = n 이면 스태시가 불가능해 플레이가 하나로 고정된다.
  공허한 단서인지 보려고 홀드 없는 모델을 전수했더니 첫 두 글자만 바꿔 답이 갈리는 수열이
  **w=4 n=4 에서만 66 쌍** 나왔다 (`OTTJ` 불가능 / `TOTJ` 가능).
  **이 불변성은 홀드가 아니라 "여분 도착 한 칸" 이 만든다** — 증명을 쓰기 전에는 몰랐던 점이다.
- **증명이 예측을 내놓았고 그 예측을 검정했다.** 1,684 건에서 상태 수까지 같았던 것은
  정리보다 강한 현상인데, 증명이 이유를 준다 — k=0 스태시 부분트리는 두 수열에서 그래프로서
  동일하고 `fix4` 가 스태시를 먼저 시도하므로 **성공 케이스는 그 안에서 끝난다.**
  반면 스태시 없는 가지는 동일하지 않고(세로 I 위에 O 를 얹은 보드는 O 를 먼저 놓아서는
  안 나온다) **실패할 때만** 훑린다. → 예측: 실패 교환쌍은 판정은 같되 상태 수가 다르다.
  확인한 1,684 건이 전부 성공이라 시험된 적이 없었다. 예측을 적고 돌렸다:

      LJTIOZS + OLSZ    no PC   38,542,538
      JLTIOZS + OLSZ    no PC   38,537,388     차이 5,150

  **판정 일치, 상태 수 불일치. 예측 통과.** 이 검정이 없었으면 "상태 수까지 항상 같다" 를
  정리의 일부로 오해했을 것이다. 정리가 말하는 것은 판정의 일치뿐이다.
- 조치: 볼트 7-bag §정리 H 에 전체 증명, docs 사본에 요약, 원자료 `data/holdswap.out`.
  검산 대장에서 `교환 불변의 증명 … 근거없음` 줄을 지우고 세 줄로 대체했다.
  논문 §7 의 "We have not proved this" 를 보조정리 + 정리 + 증명으로 올렸다.
- 영향: 볼트 7-bag·검산 대장·TODO, docs/ko, 논문, 레포 data/
- 상태: 해결

## 2026-09-19 | 추가 | 독립 구현이 w=7 을 덮었다 — 샤드 합이 4,498,382 로 떨어졌다

- 발견: 16 샤드가 전부 끝났고 **PC 합이 4,498,382**, `verify.cpp` 와 자리까지 같다.
  네 검사 항목과 "D 두 정의 mod2 불일치" 가 16 샤드 전부 0 이다.

      381584 302577 349099 289054 394804 231661 241896 272673
      314462 159075 328303 255358 188861 353583 223893 211499   합 4,498,382

- 이 한 줄이 **두 가지를 동시에** 보장한다. 루트 분할에 빠짐도 겹침도 없었다는 것과,
  독립 구현 둘이 w=7 에서도 개수까지 맞는다는 것. 분할이 틀렸으면 합이 어긋났을 것이다.
- 따라서 "독립 구현 둘이 개수까지 일치" 의 범위가 **260,423 → 4,758,805** 로 넓어졌다.
  폭 4·5·6·7·8 전부가 두 구현으로 덮인다. A 의 교차검증 조건(독립 구현 둘 + 손 유도)이
  이제 전 범위에서 채워졌다.
- 소요: 4 코어에 16 샤드, 약 5 시간. 단일 프로세스 추정은 9~19 시간이었다.
- 조치: 원자료 `data/averify-w7-shards.out`. 검산 대장 두 줄 갱신(`미완` → `확인`,
  합계 4,758,805), 볼트 결과 요약, docs t-parity-ko, README, 논문 §Computations 에서
  "w=7 은 구현 하나만" 단서를 지웠다.
- 영향: 볼트, docs/ko, README, 논문, 레포 data/
- 상태: 해결

## 2026-09-19 | 추가 | averify 에 루트 분할(`--shard`)을 넣었다

- 발견: `averify.py 7 7` 이 단일 프로세스로 3 시간 20 분을 돌고도 안 끝났다.
  진행 출력이 없어 남은 시간을 몰라서 **같은 프로그램으로 속도를 실측**했다.

      w=5 n=5     14,970 수열     49.6 초    3.3 ms/수열
      w=6 n=6    244,010 수열     29.0 분    7.1 ms/수열

  수열당 비용이 폭 하나에 2.1 배씩 붙는다. w=7 은 수열당 15 ms × 450 만 = **19 시간** 추정.
  수열 수에만 비례해도 9 시간이다. 컨테이너가 놀면 회수되므로 그대로 둘 수 없었다.
- 조치: `--shard=s/m` 을 넣었다. **루트(첫 조각)의 배치 선택지만** m 등분하고 s 번째만
  내려간다. 선택지 번호는 배치 가능 여부와 무관하게 매기므로 분할이 결정적이고 겹치지 않는다.
  **탐색·검증 로직은 한 줄도 안 건드렸고** 기본 동작도 그대로라 `make check` 에 영향이 없다.
- 검산: 작은 케이스에서 **샤드 합이 통짜 실행과 일치**해야 한다.
  w=4 n=4 는 366+350+173+216 = 1,105, w=5 n=5 는 3,786+3,259+4,650+3,275 = 14,970. 둘 다 일치.
  w=7 은 16 샤드 합이 4,498,382 여야 하고 그게 최종 검산이다.
- 16 등분한 이유: 루트 가지가 고르지 않다 (w=5 에서 샤드별 3,259~4,650). 4 등분하면 제일
  무거운 샤드가 전체 시간을 정한다. 16 개를 4 개씩 돌리면 저절로 균형이 맞는다.
- 영향: src/py/averify.py
- 상태: 해결 (w=7 실행 중)

## 2026-09-19 | 오류 | 어제 쓴 `pkill -f` 규칙을 오늘 내가 어겼다

- 발견: 실행 중인 `averify 7 7` 을 죽이려고 `pgrep -f "averify.py 7 7" | head -1` 로 PID 를
  찾아 kill 했다. 그 PID 는 **내 셸**이었다 (명령줄에 같은 문자열이 들어 있으므로). exit 144.
- 이건 2026-09-18 에 내가 직접 적어 넣은 규칙이다. **규칙을 쓴 다음 날 어겼다.**
  규칙이 `pkill -f` 만 금지하고 있어서 `pgrep -f` 로 PID 를 찾는 우회를 "안전한 방법" 으로
  착각했다. 같은 함정인데 이름만 다르다.
- 조치: 규칙을 `-f` 로 명령줄을 매치하는 `pkill`·`pgrep` 전체로 넓혔다.
  안전한 방법을 명시했다 — `ps -C <실행파일명> -o pid=,args=` 는 **실행파일 이름만** 보므로
  셸이 안 걸린다. 볼트 TODO 와 `docs/ko/INDEX.md` 양쪽에 반영.
- 교훈: 규칙을 적는 것과 지키는 것은 다르다. 금지 목록보다 **안전한 대체 명령을 같이 적는 쪽**이
  실제로 작동한다. 이번에 규칙을 고칠 때 그렇게 고쳤다.
- 영향: 볼트 TODO, docs/ko/INDEX.md
- 상태: 해결

## 2026-09-19 | 추가 | w=16 7-bag 최소 = 12 — 표의 마지막 칸이 닫혔다

- 발견: 검산 대장의 `미완` 줄 중 하나가 "w=16 ≥ 12 (n=12 미확인)" 이었다. 약 3 시간 돌렸다.
- 근거: 4n = 16R 이라 n ∈ {4, 8, 12, …}.
  n=4 는 35 개 multiset 전부 불가능(미결 0), n=8 은 7 개 전부 불가능(미결 0),
  **n=12 는 21 개 중 5 개 가능** → 최소 = 12. 노트의 `≥12` 가 등호로 바뀌었다.
- 증인 `counts=1222221` (I 1, O 2, T 2, S 2, Z 2, J 2, L 1) 을 `./bin/mset 16 1222221` 로
  9.5 초에 독립 확인했다.
- **n=12 에 미결 16 건이 남아 있지만 최소값 주장에는 영향이 없다.** 가능 증인이 다섯이고
  아래 두 줄이 미결 0 으로 배제됐기 때문이다. 미결로 남은 것은 "21 개 중 정확히 어느 것이
  되는가" 라는 별개 질문이고, 검산 대장에 그 줄을 따로 `미완` 으로 남겼다.
  (CAP 규칙대로 미결을 "불가능" 으로 읽지 않는다.)
- 조치: 원자료 `data/bagmin-16.out`. 7-bag 표의 w=16 칸, 검산 대장, TODO 갱신.
- 영향: 볼트 7-bag·TODO·검산 대장, docs/ko/seven-bag-ko.md, 레포 data/
- 상태: 해결

## 2026-09-19 | 오류 | I 위치 표의 두 줄이 재현되지 않았다

- 발견: 검산 대장의 마지막 `미완` 줄(36 케이스)을 닫으려고 전수했다. 꼬리 SZO 고정,
  앞 네 자리 {I,T,J,L} 배치 bag1 아홉 개 × TJLISZO 를 실패시키는 접두사 넷 = 36 건.
  **36 건 전부 확정, CAP·TIMEOUT 0.**
- 결과: **I 가 4 번째면 네 접두사 전부 실패, 3 번째 이하면 전부 가능.** 예외 없음.
- **옛 표의 두 줄이 틀렸다.** `TJIL·SZO 실패` 와 `LTIJ·SZO 1/4 만 실패` 는 재현하면
  둘 다 0/4 가능이다. 그 두 줄이 "I 위치만으로 결정되지 않고 J/L 배치가 함께 작용한다" 의
  유일한 근거였으므로 그 단서도 같이 내렸다. "0/4 아니면 4/4" 가설은 되살아났다.
- 근거: 같은 실행에서 TJLISZO 가 네 건 모두 **알려진 상태 수 그대로** 실패했다
  (36,891,439 / 36,824,530 / 53,417,670 / 54,046,679). 솔버 쪽 문제가 아니다.
- 원자료가 레포에 없다 — 옛 값이 어디서 나왔는지 추적할 방법이 없다.
  이번 실행분은 `data/ipos36.out` 으로 넣었다. 접두사 네 개는 전수가 아니라 탐침이고,
  여기서 "가능" 은 0/840 이 아니라 0/4 라는 점도 노트에 명시했다.
- 영향: 볼트 7-bag, docs/ko/seven-bag-order-ko.md, 검산 대장, 레포 data/
- 상태: 해결

## 2026-09-19 | 추가 | 홀드가 bag1 의 첫 두 조각을 교환 가능하게 만든다

- 발견: 위 36 건에서 `ITJLSZO` 와 `TIJLSZO` 가 네 접두사에서 **상태 수까지 한 자리도
  안 틀리고 같았다.** 우연으로 보기 어려워 기존 자료로 검정했다.
- 근거: `data/sweep100/` 에 첫 두 글자만 바뀐 bag1 쌍이 둘 있다.
  IZSOLJT/ZISOLJT 와 OTJZSLI/TOJZSLI — **각각 840 건 전부 판정도 상태 수도 동일**했다.
  36 건의 4 건까지 합쳐 1,684 건 일치, 불일치 0.
- 이유(스케치): 홀드가 빈 채로 시작하므로 첫 두 도착은 어느 쪽을 먼저 놓아도 같은 선택지를
  준다. **증명은 아직 안 썼다.** 계산 근거만 있는 상태로 노트에 적었다.
- 따름: 표본 100 개 중 실질적으로 다른 문제는 98 개. 실패 bag1 의 교환쌍도 실패하므로
  5040 전체에서 나쁜 bag1 은 짝수 개. 첫 두 자리가 같을 수 없어 5040 이 빠짐없이 짝을
  이루므로 동치류는 2,520 개 — 5040 × 840 전수 비용이 정확히 절반이 된다.
- 영향: 볼트 7-bag, docs/ko, 검산 대장, TODO(열린 문제)
- 상태: 해결 (증명은 열린 문제)

## 2026-09-19 | 오류 | docs 사본이 보조정리 M 과 모순되는 문장을 싣고 있었다

- 발견: `docs/ko/classification-ko.md` §열 프로파일 에 "실제로 w=4 에서 J 는 n=2,
  **L 은 n=4** 로 갈리는데 그 차이는 전적으로 기하에서 나온다" 가 있었다.
  같은 문서 §보조정리 M 이 "두 쌍을 구별하는 불변량은 존재할 수 없다" 고 말하는데 정면으로 어긋난다.
- 근거: `./bin/pc3 4 2 32 0` (J) 와 `./bin/pc3 4 2 64 0` (L) 이 **둘 다 FOUND**.
  `jl.py` 의 w=4 R=2 줄도 "열 해 1, J 1, L 1" 이다. w=4 최소는 J·L 모두 n=2 다.
- 이 문장은 볼트 `조각 분류.md` 에는 없다. docs 사본에만 있었다 — 사본이 원본보다
  길어진 자리에서 생긴 오류이고, 그래서 볼트 쪽 검산으로는 안 걸렸다.
- 조치: 사본에 정정 문단으로 남겼다 (지우지 않는다). 보조정리 M 절에 계산 확인을 붙였다.
- 영향: docs/ko/classification-ko.md
- 상태: 해결

## 2026-09-19 | 추가 | 미완 줄 닫기 (1) — w=7 n=7 과 보조정리 M

- 발견: 재혁이 "미완부터 닫자" 로 지정. 검산 대장의 `미완` 은 네 줄이었다
  (`verify 7 7`, `jl.py`, `bagmin.py 16`, I 위치 36 케이스).
- **`verify 7 7` 완료: PC 4,498,382 개, 네 항목 전부 0.** 4n = Rw 라 w=7 은 n 이 7 의
  배수여야 해서 최소 케이스가 (7,7) 이고, 혼자서 나머지 여덟 케이스 합의 17 배다.
  전수 합계 260,423 → **4,758,805**, 범위가 폭 4·5·6·7·8 이 됐다.
  단 **독립 구현은 아직 w=7 을 안 덮는다** (`averify.py 7 7` 실행 중). "구현 둘이 개수까지
  일치" 는 여전히 260,423 범위의 주장이고, 노트 세 곳에 그렇게 명시했다.
- **`jl.py` 완료: `identical: True`.** 덤이 하나 나왔다 — J 와 L 은 프로파일 집합만 같은 게
  아니라 **기하가 실현하는 열 해의 개수까지** 같다 (w=4 R=2·4·6·8 → 1·3·7·15,
  w=5 R=8 → 66, w=6 R=4·8 → 6·261). DIVERGE 0 줄.
  w=5 R=4 와 w=6 R=2 는 열 해 자체가 없어 출력에 없고, `colproof.py` 의 최소 R 표와 맞는다.
- 조치: `jl.py` 에 `--fast` 를 넣어 보조정리 M 확인(0.02 초)은 `make check`,
  실현 가능성 전수(약 30 분)는 `check-full` 로 갈랐다.
- 영향: 볼트(결과 요약·조각 분류·TODO·검산 대장), docs/ko, Makefile, src/py/jl.py
- 상태: 해결 (남은 두 줄은 실행 중)

## 2026-09-19 | 추가 | 검산 규칙 — `timeout` 으로 죽인 파이썬은 출력을 잃는다

- 발견: `timeout 600 python3 src/py/jl.py` 가 SIGTERM 으로 죽었는데 출력 파일이 **완전히 비어 있었다.**
  `identical: True` 는 시작 0.02 초에 계산되는데도 한 줄도 안 남았다.
- 근거: 파이썬 stdout 은 tty 가 아니면 블록 버퍼링이다. SIGTERM 기본 처리는 버퍼를 안 비운다.
  `python3 -u` 로 다시 돌리니 즉시 세 줄이 나왔다.
- 이게 위험한 이유: 출력이 비면 "아무 결론도 안 나왔다" 로 보이는데 실제로는 **결론이 났고
  버퍼에서 사라진 것**이다. CAP/빈 출력 규칙과 같은 종류의 함정이다.
- 조치: 볼트 TODO §검산 규칙과 `docs/ko/INDEX.md` 에 추가.
  오래 걸리는 파이썬은 `python3 -u` 로 돌리고, 중간 출력이 있는 프로그램에 `timeout` 을 걸 때는
  버퍼링을 먼저 끌 것.
- 영향: 볼트 TODO, docs/ko/INDEX.md
- 상태: 해결

## 2026-09-18 | 수정 | 전시 사본 동기화와 README 범위 정정

- 발견: 볼트만 갱신하고 `docs/ko` 를 안 고친 상태로 커밋이 끝나 있었다. 사본이 뒤처지면
  레포를 읽는 사람이 철회된 주장을 그대로 읽는다 (특히 "실패는 ZS 로 끝난다").
- 근거: 사본 세 종은 볼트 원본의 축자 복사(`Decisions.md`, `검산 대장.md`)이거나
  재구성 사본(`seven-bag-order-ko.md`)이다. 동기화는 볼트 → docs 한 방향이므로
  볼트를 먼저 고치고 복사했다. 이번에 docs 쪽에서 먼저 고친 것은 없다.
- 조치:
  - `decisions-ko.md`, `ledger-ko.md` 를 볼트에서 그대로 다시 복사 (헤더 주석 한 줄만 추가)
  - `seven-bag-order-ko.md` 에 표본 100 개 전수 절을 넣고, "ZS 로 끝난다" 에 반증 표시를 달고,
    실패율을 1/240 미만 → **1.1×10⁻⁴** 로 고치고, 열린 문제 4 개를 다시 썼다
  - README 에 §7 요약 문단 추가. 숫자는 전부 검산 대장에 줄이 있는 것만 썼다
  - **README 범위 정정**: 260,423 을 "widths 4-8" 이라고 써 두었는데 실제 전수 범위는
    폭 4·5·6·8 이다 (w=7 은 `verify 7 7` 미실행). 논문에서 같은 표현을 이미 고쳤는데
    README 를 빠뜨렸다
- 영향: docs/ko, README, 볼트(TODO 검산 규칙·자료 위치, 검산 대장)
- 상태: 해결

## 2026-09-18 | 추가 | 검산 규칙 두 개 — `pkill -f` 자살과 `tee | head`

- 발견: 이번 세션에 `pkill -f "bin/pc3 10 10"` 과 `pkill -f "bin/fix4"` 로 **내 셸을 두 번 죽였다**
  (exit 144). 내가 그 프로세스를 띄운 `bash -c` 의 명령줄에도 같은 문자열이 들어 있기 때문이다.
  병렬 작업에서는 자식만 죽여서 xargs 부모가 계속 새 자식을 낳는 문제도 겪었다.
- 근거: 기존 규칙 "`프로그램 | head` 는 SIGPIPE 로 조용히 깨진다" 의 변종도 실제로 밟았다.
  `colproof.py 2>&1 | tee 파일 | head -70` 에서 tee 가 SIGPIPE 로 죽어 **파일이 70 줄로 잘렸다.**
  규칙을 적어 두고도 `tee` 가 중간에 낀 형태를 못 알아봤다.
- 조치: 볼트 TODO §검산 규칙과 `docs/ko/INDEX.md` 사본에 두 항목 추가.
  PID 나 `pkill -x <실행파일명>` 을 쓰고, 병렬 작업은 부모부터 죽인다.
- 영향: 볼트 TODO, docs/ko/INDEX.md (CLAUDE.md 9 장 사본은 여전히 갱신 못 함 — 파일 접근 불가)
- 상태: 해결

## 2026-09-18 | 추가 | D 순서 제약 — bag1 100 개 전수, 실패 9 건

- 발견: 재혁이 "D 를 제대로 넣는다" 로 정했고 범위를 bag1 50~100 개 전수로 잡았다.
  시드 20260918 로 100 개를 뽑아 각각 bag2 접두사 840 개를 전수했다 (84,000 케이스).
- **1 단계의 FAIL 0 은 결과가 아니었다.** 상한 8M 에서 OK 83,359 / CAP 641 / FAIL 0 이었는데,
  641 건을 상한 200M 으로 재실행하니 **FAIL 9 건**이 나왔다. 실패 케이스는 38M~52M 상태를
  요구한다. 1 단계만 보고 "실패율 0" 이라고 썼으면 논문에 틀린 값이 들어갔다.
  2026-09-15 감사에서 만든 "CAP 은 결론이 아니다" 규칙이 실제로 값을 바꾼 첫 사례다.
- 근거: 실패 9 건은 bag1 셋에 3 건씩 몰려 있다 (LJTIOZS, LOTJISZ, TLJOISZ).
  나머지 97 개는 840/840 가능. 실패율 9/84,000 ≈ 1.1×10⁻⁴.
  **2026-09-15 에 철회한 추정치 8×10⁻⁴ 는 한 자릿수 컸다.** 철회가 옳았다.
- 관찰 세 가지:
  - **살아남음**: 실패 bag2 는 전부 {O,S,Z} + {J 또는 L}. 표본이 4 건 → 13 건으로 늘었는데 예외 없음
  - **깨짐**: "실패는 ZS 로 끝난다"(2026-09-15 관찰)는 반증됐다. 끝 두 글자가 ZS 8, SZ 2,
    OS 2, OZ 1 이고, `LSOZ` 는 bag1 에 따라 실패도 되고 가능도 된다 → 두 가방의 상호작용이다
  - **새 관찰**: 실패한 bag1 넷은 전부 S 와 Z 가 5 번째 이후에 온다. 표본 100 중 15 개가
    해당하고 그중 3 개만 실패, 조건을 어기는 85 개는 실패 0 → 필요조건 후보
- **예측을 적어두고 검정했다.** 미결이던 TIMEOUT 4 건의 bag1 은 조건을 만족하지 않으므로
  필요조건이라면 전부 가능이어야 한다고 노트에 먼저 썼고, 재실행 결과 **4 건 전부 OK** 였다.
  84,000 전건 확정, 미결 0.
- 조치: `scripts/bag1sweep.sh`(1 단계), `scripts/capresolve.sh`(2 단계) 추가. 둘 다 재개 가능.
  원자료 `data/sweep100/`, `data/cap-resolved.out`. 논문 §7 을 이 결과로 다시 썼고,
  상한이 답을 바꾼 경위도 논문에 한 문단으로 남겼다.
- 영향: 볼트, 레포, 논문, 검산 대장
- 상태: 해결

## 2026-09-18 | 추가 | 논문 초고 1 판 (9 쪽)

- 발견: C 줄기가 온전해졌고(정리 3(b), 정리 4 하한, A 독립 구현), 재혁이 인터뷰에서
  "논문 초고 먼저" 로 순서를 정했다. 분량은 Article 8~12 쪽, 집필은 AI (Projects 영역).
- 근거: 선행연구 §권장 논문 구성 + 2026-09-16 에 정한 진입점(T 의 Walkup 대비).
- 조치: `paper/tetromino-tilings.tex` 와 `paper/refs.bib` 작성. pdflatex 로 **9 쪽,
  경고 0, Overfull 0**. 볼트에 [[Projects/테트리스 T-패리티/논문|논문]] 노트를 만들어
  구성·정리 번호 매핑·수치 대조를 기록했다.
  초고의 모든 수치를 검산 대장과 대조했고 새로 들어간 수치는 없다 (CLAUDE.md 6.3).
  쓰면서 세 군데를 고쳤다 — 초록이 O 를 "모든 폭" 처럼 읽히게 쓴 것, 정리 2 의 최소성을
  셀 수 등식만으로 주장한 것, 260,423 의 범위를 "폭 4~8" 로 뭉뚱그린 것(실제는 4·5·6·8).
- 영향: 레포 paper/, 볼트
- 상태: 해결 (§7 표본 실패율은 계산 대기)

## 2026-09-18 | 정책 | 프리프린트 공개 가능 — paper/ 를 공개 레포에 둔다

- 발견: CLAUDE.md 3 장이 "첫 초고 전에 Gazette 규정 확인" 으로 남겨 둔 항목.
  재혁 답도 "규정 확인 후 결정" 이었다.
- 근거: Cambridge University Press 는 무제한 프리프린트 정책이고, Gazette 도 프리프린트
  예치를 선행/중복 출판으로 보지 않는다. 조건은 (1) 투고 원고에 프리프린트를 인용,
  (2) 게재 후 최종본으로 링크 갱신 뿐이다. (cambridge.org/core/open-research/preprint-policy)
- 조치: `paper/` 를 공개 레포에 그대로 커밋한다. `.gitignore` 에 넣지 않는다.
  빌드 산출물(.aux, .log, .out)만 무시하고 PDF 는 커밋해 프리프린트 역할을 하게 한다.
- 영향: 레포, 볼트
- 상태: 해결

## 2026-09-18 | 정책 | 다음 작업 순서와 D 의 범위 (인터뷰)

- 발견: "다음은 어떻게 하면 돼?" 에 답하기 전에 볼트를 뒤졌더니 `Portfolio/00 지도.md` 가
  나왔다. Phase B 채점표의 "포폴" 열이 MIT 포트폴리오를 가리키고 있었고, 거기 Research
  Supplement 칸이 비어 있는데 T-패리티는 후보로도 안 올라가 있었다.
- 근거: 재혁 답 (2026-09-18 인터뷰).
  - MIT Research Supplement 로는 **안 낸다**. 기한은 **내년 이후**라 여유가 있다
  - 순서는 **논문 초고 먼저**, D 계산은 백그라운드로 병행
  - D 는 **제대로 넣는다.** 단 순서 제약은 5040 중 2 개 전수뿐이라 근거가 얇다고 지적했고,
    **bag1 50~100 개 전수**로 범위를 정했다 (5040x840 전수는 이 환경에서 6 일치라 불가)
  - 분량 Article 8~12 쪽, 집필은 AI
- 조치: `scripts/bag1sweep.sh` 로 시드 고정 무작위 bag1 100 개 x 840 전수를 돌린다.
  **재개 가능하게 만들었다** — 완료된 bag1 은 건너뛰므로 컨테이너가 리셋돼도 이어서 간다
  (9 월 15~16 일에 두 번 겪었다).
- 영향: 볼트, 레포
- 상태: 해결

## 2026-09-16 | 추가 | A 의 독립 구현을 재작성했다 (철회한 주장 복구)

- 발견: 2026-09-15 감사에서 "C++ 와 Python 독립 구현이 개수까지 일치" 주장의 프로그램이
  레포에 없어 철회했고, 재작성을 다음 세션 우선순위 1 번으로 뒀다. 재혁이 지금 지정.
- 근거: `src/py/averify.py` 를 verify.cpp 와 **겹치지 않게** 썼다. 같은 알고리즘을 옮겨 적으면
  교차검증이 아니므로, 세 지점을 일부러 다르게 했다.
  - 보드: __int128 비트마스크 → 행별 정수 마스크의 튜플
  - 착지 행: 충돌 탐침(한 칸씩 내려보기) → **열 높이 프로파일 산술** `r = max(h[c] - bottom[c])`
    (테트로미노는 각 열에서 칸이 연속이라 정확하다 — 이 사실 자체가 별도 논증이다)
  - D: 줄별 합 → **"자기 아래 지워진 줄 수가 홀수인 칸의 개수"**
- 조치: 경량 7 케이스에서 개수가 전부 일치했다 — 5 / 55 / 1105 / 14,970 / 30 / 2 / 246
  (합 16,413). 네 검사 항목 전부 0. w=6 n=6 (244,010) 은 확인 중.
  결과 요약 §A 의 철회 문단을 복구 문단으로 바꾸고, 검산 대장의 `근거없음` 행을 확인으로 올렸다.
  `make check` 에 경량 3 케이스(1.6 초), `check-full` 에 나머지를 넣었다.
  w=6 n=6 도 같은 날 끝났다: **244,010 일치.** 합계 260,423 이 자리까지 맞는다.
- **뜻밖의 소득.** D 두 정의가 mod 2 로 같다는 것이 per-event 항등식에 실제로 필요한 단계인데,
  두 구현이 서로 다른 정의를 쓰므로 그 일치가 이제 **전 수열에서 기계 검증된다** (불일치 0).
  감사 때 볼트 §A 의 D 정의에서 Σ_i 가 빠져 있던 바로 그 자리이고, 그때는 서술만 고쳤다.
  이제 그 단계에 코드 근거가 붙었다.
- 영향: 볼트, docs, README, 코드, 검산 대장
- 상태: 해결

## 2026-09-16 | 추가 | 정리 4 최소성 하한이 임의 폭에서 닫혔다 (전 조각 최소 R 표까지)

- 발견: 정리 3(b) 직후 재혁이 "정리 4 하한도 지금 닫아줘" 로 지정.
- 근거: T 에서 쓴 "앞 세 열 방정식" 을 J/L 에 그대로 쓰려니 경계까지 따라가야 해서 길어진다
  (R=2 에서 열 방정식이 주기 4 패턴을 강제하고, w≡2 mod 4 일 때 오른쪽 끝에서 모순이 난다).
  그래서 손 유도 대신 **임의 폭을 한 번에 판정하는 도구**를 만들었다.
  열 카운팅을 왼쪽에서 오른쪽으로 훑는 전이의 상태는 "다음 열들로 넘긴 기여" 이고 각 성분이
  0..R 이라 상태 공간이 유한하다. 상태집합 위의 전이가 결정적이므로 도달 집합 수열이 결국
  주기적이고(비둘기집), 오른쪽 끝 carrylen 개 열의 변환열은 폭에 무관하게 고정이다.
  → "폭 w 에서 열 해가 존재하는가" 가 w 에 대해 결국 주기적. 주기를 검출하면 전 폭이 결정된다.
- 조치: `src/py/colproof.py` 추가. 결과는
    J·L : R=1 전 폭 불가 / R=2 는 4|w / R=4 는 짝수 폭 / R=8 은 전 폭 → 최소 2·4·8
           → n = w/2 (4|w), w (w≡2 mod 4), 2w (홀수). **정리 4 의 표와 일치.**
    I : 4|w 에서 R=1, 그 외 R=4       O·S·Z : 짝수 폭 R=2, 홀수 폭 열 해 없음
    T : 모든 폭 R=4 (정리 3(b) 하한을 독립 도구가 재확인)
  조각 분류 §정리 4 최소성과 docs 를 "임의 폭 증명" 으로 고쳤다. 검산 대장 3 줄 등록.
- 교차검증: 주기 논증 대 직접 계산(colcount.py 와 같은 계산)이 폭 4..40 × R 1..12 × 7 조각에서
  불일치 0 건. T 쪽은 손 유도·세 열 전수·colproof 세 경로가 같은 결론.
- 부수 결론 둘:
  - **최소 n 의 완전한 표가 하한 쪽에서 완결됐다** (TODO 의 열린 문제 하나가 닫혔다).
  - S·Z 의 짝수 폭 R=2 통과가 임의 폭에서 확정됐으므로, 정리 1(b) 의 기하 논증이
    **모든 짝수 폭에서 필요**하다는 것도 임의 폭에서 확정이다. 카운팅만으로는 절대 안 닫힌다.
- 영향: 볼트, docs, README, 코드, 검산 대장
- 상태: 해결

## 2026-09-16 | 정책 | 논문 진입점을 T 의 Walkup 대비로 올린다

- 발견: 정리 3(b) 를 닫고 나니 "정적 제약이 막는 만큼을 동적 규칙이 채운다" 가
  T 의 유닛 분해에서 가장 선명하게 드러난다. 기존 §권장 논문 구성은 이 대비를
  "4 주결과" 의 따름 정도로만 두고 있었다.
- 근거: 재혁 결정 (2026-09-16 인터뷰). 선택지는 서론 진입점 승격 / 정리 3(b) 절 안의 주석 /
  별도 절 독립 / 초고 쓸 때 결정. **서론 진입점 승격**을 골랐다.
- 조치: 선행연구 §권장 논문 구성 1 서론에 명시하고 T 임의 폭 §Walkup 과의 관계로 링크.
- 영향: 볼트, docs
- 상태: 해결

## 2026-09-16 | 정책 | 다음 세션 우선순위와 이번 문제의 재미 점수

- 발견: Phase B 채점표에서 재미는 재혁 몫이고, 이번 탐구가 끝났으니 점수와 다음 순서를 받았다.
- 근거: 재혁 답 (2026-09-16 인터뷰).
  - **재미 3 (보통)** — 앞으로의 상대 기준. 이번 문제는 감사·기록 일이 많았다.
  - 다음 세션: **(1) A 독립 구현 재작성, (2) 정리 4 최소성 하한 닫기.**
    정리 4 하한은 "다음 세션에 바로" 로 답했고, 체크한 첫 항목은 A 독립 구현이다.
    둘 다 다음 세션이고 A 가 먼저다.
- 조치: TODO 머리에 §다음 세션 우선순위로 박았다.
  Phase B 표의 논문가치·포폴 점수는 내 채점이고 재미 3 은 재혁 채점이다.
- 영향: 볼트
- 상태: 해결

## 2026-09-16 | 추가 | 정리 3(b) — T 단독 임의 폭이 닫혔다

- 발견: 2026-09-15 감사가 낸 구멍(T 만 임의 폭 정리 없음)을 탐구 후보 1 번으로 골라 진행.
- 근거: 유닛 분해가 정리 4 와 같은 형태로 통했다.
  - **하한**: T 의 프로파일이 (1,2,1), (1,3), (3,1) 셋뿐이라 앞 세 열의 카운팅 방정식만으로
    R = 1, 2, 3 이 전부 모순이다 → R ≥ 4 → n = Rw/4 ≥ w. 임의 폭에서 성립하는 손 유도이고
    세 열 방정식의 정수해를 전수로 세어 교차검증했다 (R=1·2·3 → 0 개, R=4 → 10 개).
  - **상한**: 폭 4 적층 유닛 `U@0 L@2 R@0 D@1` 을 a 개 + 마지막 유닛 폭 m ∈ {4,5,6,7}.
    w = 4a + m 으로 모든 w ≥ 4 를 덮고 조각 수가 4a + m = w 로 하한과 일치한다.
  - 실질적 관문은 **4 × 4 T 타일링이 중력 아래 순수 적층으로 되는가** 였다. Walkup 은
    정적 타일링만 보장하고 (L2) 지지 조건은 말하지 않는다. 된다 — 위 배치열이 그것이다.
  - **Walkup 과의 대비가 여기서 가장 선명하다**: 조건 (a) 가 x × 4 직사각형의 T 타일링이라
    적층 유닛 폭이 4 의 배수로 강제되고, 정적 타일링이 막는 폭 5·6·7 을 마지막 유닛의
    줄 클리어가 정확히 그만큼 메운다. 논문의 중심 문장으로 쓸 자리다.
- 조치: 새 노트 [[Projects/테트리스 T-패리티/T 임의 폭|T 임의 폭]] 에 유도·검산·인터뷰를 기록.
  조각 분류 §정리 2,3 에 T 를 넣고 완전 분류 근거표를 갱신. 결과 요약 §문서에 링크.
  코드 `src/py/tunits.py` (탐색·조립·하한 전수) 와 `src/cpp/tplay.cpp` (독립 재생기) 추가.
  검산 대장에 6 줄 등록.
- 교차검증: **세 번을 채웠다.**
  구성 = tunits.py(Python) / tplay.cpp(C++ 독립, 비트마스크 미사용) / 손 계산(폭 4 유닛 추적).
  하한 = 손 케이스 분석 / 세 열 방정식 정수해 전수 / colcount.py(폭 4~12 의 최소 R = 4).
- 영향: 볼트, docs, README, 코드, 데이터, 검산 대장
- 상태: 해결

## 2026-09-16 | 정책 | T 를 정리 3 에 합친다

- 발견: 새 정리에 번호를 줘야 하는데 기존 1~5 가 열려 있는 대로 부여돼 있었다.
- 근거: 재혁 결정 (2026-09-16 인터뷰). 선택지는 정리 3 에 합치기 / 정리 6 신설 /
  정리 2·3 사이에 끼워넣고 뒤 번호 밀기. **정리 3 에 합치기**를 골랐다.
  정리 3 이 I 하나만 다루고 있었고, 이러면 기존 번호가 하나도 안 밀린다.
- 조치: 절 제목을 "정리 2, 3 (O, I, T)" 로 고치고 I 를 3(a), T 를 3(b) 로 표기.
  docs/ko·README 도 같은 표기로 맞췄다. 논문에서는 재번호한다.
- 영향: 볼트, docs, README
- 상태: 해결

## 2026-09-16 | 보류 | 정리 4 의 최소성은 T 보다 근거가 약하다

- 발견: T 의 하한을 임의 폭에서 손으로 유도하고 나서 정리 4 를 다시 보니 강도가 다르다.
- 근거: 조각 분류 §정리 4 의 최소성은 "카운팅 최소 R 과 일치하므로 최적 + 폭 4..60 기계 검증" 이다.
  구성(상한)은 유닛 검산으로 w 에 의존하지 않지만, **하한은 폭별 기계 검증**이다.
  T 는 이번에 앞 세 열 방정식으로 임의 폭 하한을 닫았으므로 같은 방법이 J/L 에도 통할 수 있다.
- 조치: 고치지 않았다. TODO 열린 문제에 올렸다. 논문에서 두 정리의 강도를 같게 맞추는 게 좋다.
- 영향: 없음 (기록)
- 상태: 보류

## 2026-09-15 | 오류 | pc3 가 넓은 폭에서 거짓 음성을 냈다 (검산 규칙 1 번)

- 발견: T 단독 확인 범위를 넓히려고 폭 13~20 을 돌렸더니 w=18·19·20 이 "NO perfect clear" 였다.
  T 가 폭 14 까지 되는데 18 에서 갑자기 안 된다는 건 수상하다 → 끝까지 추적했다.
- 근거: `bitindex(r,c) = r*w + c` 의 최대값은 hcap*w-1 이고, pc3 의 메모 키는
  `kk = (b2 << 8) | meta` 로 보드를 8 비트 더 왼쪽으로 민다. 따라서 **hcap*w ≤ 120** 이어야 한다.
  T 단독은 hcap = totclear+4 = 8 이므로 w=18 이면 144, w=20 이면 160 으로 128 비트를 넘는다.
  상위 비트가 잘려 서로 다른 상태가 같은 키를 갖고, 해가 있는 가지를 중복으로 보고 잘라낸다
  → **거짓 "NO"**. 이게 바로 볼트 검산 규칙 1 번("hcap × w 가 64 를 넘으면 __int128 필수")이
  가리키던 함정의 __int128 판이다.
- 조치: 세 결과(w=18·19·20)를 **폐기**하고 검산 대장에 폐기 사유를 적었다.
  `pc3` 에 `hcap*w > 120` 이면 REFUSED 를 찍고 종료 코드 2 로 빠지는 가드를 넣었다.
  `mset`·`verify`·`seq`·`seqp` 에도 같은 취지로 `hcap*w > 128` 가드를 넣었다 (이들은 키를 안 민다).
  버그 수정이므로 기존 코드에 손댔다. `make check` 는 여전히 전항목 통과.
- 영향: 코드, 검산 대장, 볼트, docs, README
- 상태: 해결. **부수 결론**: T 단독은 pc3 로 w=15 까지만 볼 수 있으므로
  "모든 폭" 은 탐색으로 닫을 수 없다 — 구성·증명이 유일한 길이다 (탐구 후보 1 번의 근거 보강)

## 2026-09-15 | 오류 | 내가 빈 출력을 검출하지 않았다 (검산 규칙 2 번)

- 발견: 위 폭 13~20 실행의 출력에 w=15·16·17 줄이 **아예 없었다.**
- 근거: 내 셸 루프가 `timeout 1200 ./bin/pc3 … | sed -n 1p` 였고 타임아웃이면 빈 문자열이 된다.
  프로젝트가 이미 적어 둔 검산 규칙 2 번("셸 루프에서 timeout 의 빈 출력을 반드시 검출.
  빈 출력 ≠ 불가능")을 내가 그대로 밟았다. 다행히 빈 줄이라 "불가능" 으로 오독하진 않았지만,
  결과가 조용히 사라진 것은 같은 결함이다.
- 조치: w=15 재실행 스크립트에 빈 출력 검출을 넣어 `TIMEOUT or EMPTY OUTPUT -- not a result` 를
  찍게 했다. w=16·17 은 어차피 가드가 REFUSED 로 거부한다.
- 영향: 없음 (절차)
- 상태: 해결

## 2026-09-15 | 오류 | "세 조각 이상은 자명하게 가능" 이 틀렸다

- 발견: 완전 분류표의 한 줄을 반례로 시험. {O,S,Z} 는 세 종류인데 셋 다 홀수 폭에서 실패한다.
- 근거: 세 종의 프로파일 집합이 {(1,2,1),(2,2)} 로 **S 단독과 같다.** 그래서 정리 1(a) 의
  카운팅이 글자 그대로 적용되고 O 의 (2,2) 도 짝수 열로 강제된다 → 홀수 폭에서 R = 0.
  `./bin/pc3 <w> <w> 26 0` 으로 w=5, 7, 9 전부 NO, w=6 은 FOUND (O 만 쓰면 된다).
  논증의 구멍은 "불가능 쌍이 하나뿐" 에서 3 종 이상을 결론한 부분이다 — O 자체가 폭 제약을
  가지므로 O·S·Z 만 받은 경우가 남는다.
- 조치: 볼트 조각 분류.md → docs/ko/classification-ko.md → README 세 곳을 고쳤다.
  대신 부분집합 전체를 덮는 강한 형태로 다시 썼다:
  **P 가 폭 w 에서 PC 가능 ⟺ P ∩ {I,T,J,L} ≠ ∅, 또는 (w 짝수이고 O ∈ P).**
  2 종·3 종·2^7 부분집합을 한 문장으로 덮으므로 원래 표보다 강하다.
- 영향: 볼트, docs, README, 검산 대장
- 상태: 해결

## 2026-09-15 | 오류 | T 만 임의 폭 정리가 없다

- 발견: 6.3 대조. 분류표에서 T 칸의 근거 열이 비어 있었다. 정리 1~5 를 훑어보니
  1·5 는 S·Z, 2 는 O, 3 은 I, 4 는 J·L 이고 **T 를 다루는 정리가 없다.**
- 근거: "T 모든 폭 가능 (n = w)" 의 실제 근거는 폭별 탐색(대조표는 w=4~10)과 n = w 패턴 관찰뿐이다.
  임의 폭 구성(정리 4 의 유닛 분해 같은 것)이나 증명이 노트에 없다.
  이번 세션에 폭 11·12 를 추가로 확인했고 13~20 을 돌리고 있다.
- 조치: "모든 폭" 단정을 확인 범위 표기로 바꿨다 (볼트·docs·README).
  **정리 진술을 바꾼 게 아니라, 정리가 없다는 사실을 표에 명시한 것이다.**
  논문의 중심 대비(Hochberg 하한 무효화)가 T 에 걸려 있으므로 분류에서 가장 약한 자리다.
  임의 폭 T 구성을 Phase B 탐구 후보 1 번으로 올렸다.
- 영향: 볼트, docs, README, 검산 대장
- 상태: 해결 (증명은 미해결 — 탐구 후보)

## 2026-09-15 | 추가 | w=10 최소 = 10 의 재현 경로를 바꿨다

- 발견: 검산 대장에 `pc3 10 10 127 1` 을 등록해 돌렸더니 23 분에 RSS 10.2 GB 까지 갔다
  (15 GB 환경). `pc3` 는 BFS 프런티어를 깊이마다 전부 들고 있어 전 조각 w=10 n=10 은 안 끝난다.
- 근거: 이 주장은 두 조각으로 나뉜다. **최소성**은 `pc3 10 5 127 1` = NO 로 이미 확인됐고,
  **달성**은 구성 하나만 보이면 된다. MSE 의 반례가 T 다섯 + L 다섯이므로
  `mset 10 0050005` 로 물으면 된다 (#T = 5 로 홀수 조건도 자동 충족).
- 조치: 프로세스를 중단하고 검산 대장의 명령을 multiset 경로로 바꿨다. **1.8 초에 PC POSSIBLE.**
  `pc3 10 10` 은 이 주장에 쓰지 말라고 대장에 명시했다.
- 영향: 검산 대장
- 상태: 해결

## 2026-09-15 | 오류 | 7-bag 순서 제약 "837 가능 / 4 불가능"

- 발견: 노트 간 대조 (7-bag.md ↔ 선행연구.md) + 산술.
- 근거: 837 + 4 = 841 ≠ 840. `data/tjliszo-840.out` 은 834 OK + 6 CAP 이고,
  그 6 후보 중 2 개가 가능(SLOZ, LSOZ)이므로 834 + 2 = **836**.
  선행연구.md 는 이미 "836/840" 이라고 적고 있었고, docs/ko 도 "나머지 834 가능" 이라 자기모순이었다.
- 조치: 볼트 7-bag.md → docs/ko/seven-bag-order-ko.md 를 836 으로 수정. README 해당 없음.
- 영향: 볼트, docs
- 상태: 해결

## 2026-09-15 | 오류 | "bag1=IOTSZJL 840 전수 전부 가능" 의 근거가 CAP 였다

- 발견: 원자료 대조. `data/b2all.out` 이 836 OK + **4 CAP**.
- 근거: CAP = INCONCLUSIVE = 상태 상한 도달이고 "가능" 이 아니다.
  docs/ko 에는 "상한 40M 으로 재실행 후 전부 가능" 이 적혀 있었으나 그 재실행 결과가 파일로 없었다.
- 조치: 네 건(OJSZ, JOSZ, JSOZ, JSZO)을 `fix4 … 200000000` 으로 재실행 →
  전부 PC POSSIBLE. 결과를 `data/iotszjl-4cap-fix4.out` 으로 커밋하고 노트에 경위를 명시.
  검산 규칙에 "CAP 은 결론이 아니다" 를 추가했다.
- 영향: 볼트, docs, 데이터
- 상태: 해결

## 2026-09-15 | 오류 | 정리 5 검증 개수 7,141 을 합계처럼 적었다

- 발견: 층위 1 재실행. 노트는 "H = 14, 16, 18 의 도달 가능 블록 상태 전부 (7,141 개)".
- 근거: H=12·14·16·18 의 상태 수는 397 / 1,041 / 2,727 / **7,141**.
  7,141 은 H=18 단독이고 세 H 의 합은 10,909 다. docs 는 "최대 7,141" 로 정확했다.
- 조치: 볼트 조각 분류.md 와 docs 를 H 별 수치로 고쳤다. 검산 규칙에 "합인지 최대인지 명시" 추가.
- 영향: 볼트, docs
- 상태: 해결

## 2026-09-15 | 오류 | A 항등식의 D 정의에서 Σ_i 가 빠졌다

- 발견: 층위 2 독립 재유도. 노트를 보기 전에 D 를 유도했더니 "s(r) 이 홀수인 칸의 개수" 가 나왔다.
- 근거: 결과 요약.md 는 "D = 지워진 줄들 위의 칸 수 (꽉 찬 줄 제외)" 로 한 번만 세는 것처럼 읽힌다.
  k ≥ 2 다중 클리어에서 틀린다. docs/ko/t-parity-ko.md 는 "D = Σ_i (줄 j_i 위의 칸 수…)" 로
  정확했고, 각 칸이 s(r) 번 세어지므로 mod 2 로 내 유도와 일치한다.
- 조치: 볼트 결과 요약.md 에 Σ_i 와 "줄마다 세어 합한다" 를 복원. docs 는 이미 정확.
- 영향: 볼트
- 상태: 해결

## 2026-09-15 | 오류 | 실패율 8×10⁻⁴ 는 근거가 없다 → 철회

- 발견: 근거 추적. "무작위 bag1 6 개 중 1 개가 나빴고" × "4/840" = 0.17 × 0.0048.
- 근거: docs/ko 에는 "무작위 8 개 30 씩 → 239/240" 으로 다른 표본이 적혀 있다.
  전수한 bag1 은 IOTSZJL·TJLISZO 둘뿐이고, "나쁜 bag1 의 비율" 은 어디에도 전수 근거가 없다.
  두 수를 곱한 값은 통계적으로 뒷받침되지 않는다.
- 조치: 곱셈 추정을 지우고 표본 내역을 정확히 적었다 (전수 두 bag1 기준 4/1680 ≈ 2×10⁻³,
  표본 기준 1/240 미만). **논문 인용 금지**를 노트와 검산 대장에 명시.
- 영향: 볼트
- 상태: 해결

## 2026-09-15 | 오류 | "C++ 와 Python 독립 구현 일치" 주장의 프로그램이 없다 → 철회

- 발견: 근거 추적. 결과 요약.md §A 의 문장.
- 근거: `src/py/` 에 PC 수열을 세는 프로그램이 없다. A 의 전수검증은 `verify.cpp` 단독이다.
  따라서 검산 규칙의 "독립 구현 둘 + 손 계산" 조건을 A 는 아직 못 채웠다.
- 조치: 주장을 철회하고, 독립 구현 재작성을 TODO 열린 문제로 옮겼다. 검산 대장에 `근거없음` 으로 등록.
- 영향: 볼트, docs
- 상태: 해결 (재작성은 열린 문제)

## 2026-09-15 | 수정 | 정리 1(b) 와 정리 5 I3 의 빠진 단계 보강

- 발견: 층위 2 재유도 중 두 곳에서 단계가 비어 있었다.
- 근거: 정리 1(b) 는 "(0,0) 이 영원히 빈다" 를 배치 논증만으로 말하는데, **낙하**로 칸이
  행 0 에 들어올 가능성을 따로 막아야 한다 (행 0 클리어가 (0,0) 을 요구하므로 순환 → 동시 귀납).
  정리 5 I3 는 "d_0 은 r=0 착지로만 바뀐다" 고 하는데 **클리어**가 d_0 을 바꾸지 못함도 필요하다
  (m>0 이면 d_0 불변, m=0 은 d_0=0 을 요구해 귀납 가정이 배제).
- 조치: 두 곳에 한 단계씩 추가. **진술과 결론은 건드리지 않았다** (증명 보강만).
- 영향: 볼트, docs
- 상태: 해결

## 2026-09-15 | 오류 | 내 축약 유도가 틀렸다 (정리 5)

- 발견: 층위 2. 정리 1(b) 의 "첫 조각이 행 0 에 구멍을 남긴다" 논증이 {S,Z} 에도
  그대로 통한다고 생각해 I0~I3 가 불필요하다고 적었다.
- 근거: 세로 Z 는 **왼쪽(짝수) 열이 상대행 0 에 닿는다.** 그래서 블록의 구멍이 어느 쪽에
  생기는지가 조각에 따라 달라지고, 클리어로 블록 상태가 변하므로 상태 불변량이 실제로 필요하다.
  노트의 증명이 맞고 내 축약이 틀렸다.
- 조치: 노트를 고치지 않았다. 재유도 기록(`independent-derivation.md`)에 내 오류로 남겼다.
  6.4 의 "클로드의 재유도가 틀렸을 가능성을 먼저 의심한다" 가 실제로 적용된 사례.
- 영향: 없음
- 상태: 해결

## 2026-09-15 | 추가 | szinv.py — 불변량 I0~I3 검산기가 없었다

- 발견: 노트가 "I0~I3 위반 0" 을 주장하는데 `szlang.py` 는 상태·단어만 세고 불변량을 검사하지 않는다.
  README Layout 은 szlang.py 를 "invariant checks" 로 잘못 설명하고 있었다.
- 근거: 코드 확인. 검산 대장에 올릴 명령이 없는 숫자는 결함 3 번이다.
- 조치: `src/py/szinv.py` 를 **szlang.py 와 독립 구현**으로 새로 썼다 (교차검증 겸용).
  상태 수가 네 H 에서 모두 일치(397/1041/2727/7141)하고 위반 0.
  `make check` 에 넣고 README 설명을 고쳤다.
- 영향: 코드, README, 볼트, docs
- 상태: 해결

## 2026-09-15 | 추가 | bagmin.py — 7-bag 최소 조각 수 표를 만드는 프로그램이 없었다

- 발견: CLAUDE.md 7 장이 "프로그램 확인 필요" 로 남겨둔 항목.
- 근거: `mset.cpp` 는 multiset 하나를 판정하고 `bag.py` 는 w=10 의 두 경우만 훑는다.
  폭 4..16 표 전체를 만드는 도구가 없었다.
- 조치: `src/py/bagmin.py` 추가 (7-bag 합법 multiset 전수 열거 → mset 호출).
  폭 4·5·6·7·8·9·10·11·12 에서 표를 재현했다 (1·5·3·7·4·9·10·11·9).
- 영향: 코드, 검산 대장
- 상태: 해결 (w=13·14 실행 중, w=16 미실행)

## 2026-09-15 | 수정 | make check 의 조용한 실패와 범위 과장

- 발견: 층위 1 실행. `make check` 가 exit 0 인데 출력에 BrokenPipeError 가 있었다.
- 근거: Makefile 의 `python3 src/py/szlang.py | head -20` 에서 head 가 먼저 닫히며 SIGPIPE.
  파이프라인 종료 상태는 head 것이라 make 가 성공으로 본다. 6.6-2 의 범위 과장도 확인
  (다만 과장 문구는 README 가 아니라 Makefile 주석 "reproduce the main claims" 였다).
- 조치: 모든 `| head -N` 을 `sed -n` 으로 교체(입력을 끝까지 읽으므로 SIGPIPE 없음).
  `check` 를 정리 1·2·4·5 + A 경량 전수 + B(w=4) + T 패리티 인자 함정까지 확장하고,
  느린 항목은 `check-full` 로 분리했다. 검산 규칙에도 추가.
- 영향: 코드, README, 볼트, docs
- 상태: 해결

## 2026-09-15 | 수정 | 없는 솔버를 부르는 스크립트

- 발견: CLAUDE.md 6.6-1. `survey.sh`·`sv2.sh` → `./fix2`, `runb2.sh` → `./fix`.
- 근거: `fix2.cpp`, `fix.cpp` 가 레포·git 이력·컨테이너 전체 어디에도 없다.
  게다가 바이너리가 이제 `bin/` 에 있어 `./fix2` 경로 자체도 틀렸다.
- 조치: 기본 솔버를 `./bin/fix4` 로 바꾸고 `SOLVER`·`CAP`·`TIMEOUT` 환경변수로 바꿀 수 있게 했다.
  네 스크립트에 머리 주석(용도·사용법·출력)을 넣었다.
- 영향: 코드, README, 볼트 TODO
- 상태: 해결

## 2026-09-15 | 수정 | 컨테이너 경로와 죽은 재현 안내

- 발견: CLAUDE.md 6.6-4. 볼트 TODO §자료 위치가 `/mnt/user-data/outputs/tetris-parity/`.
- 근거: 그 경로는 세션마다 초기화되는 옛 컨테이너 경로다. 결과 요약.md §문서에도 같은 경로가 있었다
  (6.6 목록에는 TODO 만 적혀 있었다).
- 조치: 두 곳 모두 레포 경로로. TODO 에는 파일별 용도 목록과 fix2/fix 부재를 명시했다.
  docs/ko/t-parity-ko.md 의 재현 블록도 `g++ -o verify …` → `make` 로 고쳤다.
- 영향: 볼트, docs
- 상태: 해결

## 2026-09-15 | 수정 | docs/ko 가 자기를 원본이라고 말하고 있었다

- 발견: CLAUDE.md 6.6-3. INDEX.md "여기 있는 파일이 실제 작업 기록이다".
- 근거: 3 장에 따라 볼트가 원본이고 docs/ko 는 전시 사본이며 동기화는 한 방향이다.
- 조치: INDEX.md 를 "이 폴더는 전시 사본" 으로 고치고 볼트 경로와 방향을 명시.
  이번 감사에서 나온 검산 규칙 3 개도 INDEX 사본에 추가했다.
- 영향: docs
- 상태: 해결

## 2026-09-15 | 추가 | 서지 보강 (층위 3)

- 발견: 인용 상당수에 권·쪽이 없었다. CLAUDE.md 는 "인용 8 건" 이라 하지만 실제로는 12 건이다.
- 근거: 웹 검색으로 확인 — Walkup 1965 (AMM 72, 986-988) / Hochberg 2015 (**Discrete Math 338,
  130-138**) / Korn-Pak 2004 (**TCS 319, 3-27**) / Reid 2005 (**JCTA 111(1), 89-105**) /
  Burgiel 1997 (Math. Gazette 81, 194-200) / MIT Hardness Group 2026 (**FUN 2026, LIPIcs 366:32**) /
  Torres-Vallejo 2024 (**SciEnggJ 17(1), 128-133**).
  MIT 2026 에서 새로 확인된 것: **7k-bag 랜더마이저 하의 clearing NP-hard 가 따름정리로 있다**
  (우리 D 절과 직접 닿는 유일한 최신 문헌), I 조각에 대한 23 년 된 추측 반증.
- 조치: 볼트 선행연구.md, docs/ko/literature-ko.md, README References 보강.
  MIT 2026 의 7k-bag 따름정리는 "논문에서 반드시 언급" 으로 표시하고 TODO 에 올렸다.
  Torres-Vallejo 의 이름 이니셜은 확인하지 못해 성만 적었다 (추측하지 않음).
- 영향: 볼트, docs, README
- 상태: 해결

## 2026-09-15 | 보류 | Hoogeboom-Kosters 2004 쪽수

- 발견: 층위 3. README 와 docs/ko 는 `IJIGS 3(2), 97-105`, 웹 검색 결과는 `94-102`.
- 근거: 원문 PDF(liacs.leidenuniv.nl), dblp, arXiv 가 이 세션의 네트워크 정책상 접근 차단이다.
  내부 사본 둘이 97-105 로 일치하고 반대 근거는 검색 스니펫 하나뿐이다.
  6.4 의 "세 번 교차 검증 전에는 오류라고 부르지 않는다" 에 따라 **어느 쪽도 고치지 않았다.**
- 조치: 양쪽 값을 노트에 병기하고 TODO 에 `미조사` 로 올렸다. 투고 전 원문 확인 필요.
- 영향: 볼트, docs
- 상태: 보류 (재혁 판단 / 로컬에서 원문 확인)

## 2026-09-15 | 수정 | 재현 불가 자산 판정을 기각했다

- 발견: CLAUDE.md 6.6-1 은 fix2/fix 가 없으면 줄기 D 순서 제약을 "재현 불가 자산" 으로
  기록하고 그 수치를 README·논문에서 인용하지 말라고 했다.
- 근거: 노트 표의 상태 수 최대값 54,046,679 가 fix4 의 내부 한계 93,952,404 보다 작다.
  즉 그 표는 완전 탐색 결과이고 fix4 로 재현 가능해야 한다. 실제로 6 건 전부
  상태 수까지 정확히 일치했다 (36,891,439 / 36,824,530 / 53,417,670 / 54,046,679 /
  22,581,411 / 30,349,604).
- 조치: **재현 불가 자산 아님.** fix2/fix 는 예비 선별용 솔버였고 확정 결과는 fix4 가 만들었다.
  재현 명령을 검산 대장에 등록하고 결과를 `data/*-fix4.out` 으로 커밋했다.
  README·논문 인용 제한도 걸지 않는다. 단 `.out` 두 파일의 CAP 행은 결론이 아니라고 명시했다.
- 영향: 볼트, docs, 데이터, 검산 대장
- 상태: 해결

## 2026-09-15 | 수정 | 머리 주석과 README Layout

- 발견: CLAUDE.md 6.6-8. `src/cpp/*.cpp` 7 개 전부 머리 주석 없음.
  `seq.cpp`·`seqp.cpp` 는 README Layout 에도 없었다.
- 근거: 코드를 읽어 용도를 확인했다. seq 는 고정 수열 PC 탐색 + 단계별 보드,
  seqp 는 거기에 phi 와 클리어 보정항 추적을 더한 A 항등식 손검산 도구다.
- 조치: C++ 7 개 + Python 10 개에 용도·입력·출력 3 줄 주석. README Layout 에 전 프로그램 등재.
  `szlang.py` 의 "꽉 찬 줄 부분집합 클리어" 가 블록 독립성 때문의 **의도적 과대근사**임을
  주석으로 박았다 (모델 (L3) 위반으로 오독될 자리였다).
- 영향: 코드, README
- 상태: 해결

## 2026-09-15 | 확인 | 결함이 아니었던 것들

- 발견: 6.6 목록과 6.3 대조 체크리스트를 돌린 결과.
- 근거와 결론:
  - **정리 1(b) 서술 (6.6-7)**: 볼트·docs/ko·README 세 곳 모두 "오른쪽 열만 상대행 0" 방향으로
    이미 고쳐져 있다. 내 독립 유도와도 일치. 정상.
  - **CITATION.cff (6.6-9)**: repository-code 가 git remote 와 일치하고 authors 도 실제 값.
    SETUP.md 가 고치라던 항목은 이미 반영돼 있다. 정상.
  - **백그라운드 프로세스 (6.6-10)**: 이 원격 Linux 세션에서는 툴 호출 사이에 죽지 않는다.
    fix4 9 건, pc3 6 건, bagmin 을 백그라운드로 돌려 확인. 청크 분할 불필요 (이 환경 한정).
  - **w=4 최소 반례**: 노트는 O–T–J, `pc3 4 3 127 1` 은 O–T–L 을 출력한다.
    보조정리 M 의 거울쌍이고 pc3 는 마스크가 거울에 닫힐 때 좌우 정규화를 하므로
    출력이 한쪽으로 몰린다. 불일치 아님 — 검산 대장에 주석으로 남겼다.
  - **fix2 의 2.6% 상태 과다**: docs/ko 에 원인(mask2 갱신 경로 차이)이 이미 규명돼 있다.
    fix2 소실로 재현은 불가하나 결론에 영향 없다.
  - **7-bag 표의 제약없음 행**: w/gcd(w,4) 와 폭 4..16 전부 일치. 산술 정상.
  - **260,423**: docs 표의 8 개 값 합이 정확히 260,423. 산술 정상.
- 영향: 없음
- 상태: 해결
